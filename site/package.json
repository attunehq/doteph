{
  "name": "eph-site",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Landing page and docs site for eph (attunehq.github.io/doteph).",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource-variable/instrument-sans": "^5.2.6",
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.3.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
